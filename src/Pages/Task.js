import React from 'react';
import Footer from '../Components/Footer';
import Header from '../Components/Header';

const Task = () => {
    return (
        <div>
            <Header></Header>
            <h1>Task</h1>
            <Footer></Footer>
        </div>
    );
};

export default Task;